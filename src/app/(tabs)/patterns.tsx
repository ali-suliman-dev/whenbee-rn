import { Screen } from '@/src/components/Screen';
import { ScreenHeader } from '@/src/components/ScreenHeader';

export default function Patterns() {
  return (
    <Screen>
      <ScreenHeader
        title="Patterns"
        subtitle="Your habit and trend insights live here."
      />
    </Screen>
  );
}
