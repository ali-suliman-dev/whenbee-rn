<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="expo.modules.whenbeepresence">

    <!-- Auto-granted for timer/alarm apps on API 33+ — no runtime request needed.
         Lets the exact overrun alarm fire while the app is backgrounded/killed. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- API 31/32 counterpart of USE_EXACT_ALARM. Hardens setExactAndAllowWhileIdle so
         the overrun flip stays exact on Android 12/12L (falls back gracefully if denied). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application>
        <receiver
            android:name=".TimerAlarmReceiver"
            android:exported="false" />

        <!-- Native Home-screen widget (RemoteViews). -->
        <receiver
            android:name=".NextTaskWidgetProvider"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_task_widget_info" />
        </receiver>
    </application>
</manifest>
