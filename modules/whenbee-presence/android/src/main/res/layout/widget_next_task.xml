<?xml version="1.0" encoding="utf-8"?>
<!-- Honest Finish (next-task) Home-screen widget. Presentation-only: renders the
     snapshot JS wrote (see WhenbeePresenceModule.writeWidgetSnapshot /
     src/services/liveActivity.ts). Colors mirror src/theme/tokens.ts (dark surface).
     Hero-clock layout: eyebrow + title up top, honest-finish time as the hero
     (mono, ink white — meridiem colored inkSoft in code, never amber: amber is
     reserved for the one CTA), flat square play button bottom-right. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="12dp">

    <!-- Eyebrow -->
    <TextView
        android:id="@+id/widget_eyebrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_next_up"
        android:textColor="#ADA9B5"
        android:textSize="10.5sp"
        android:letterSpacing="0.03"
        android:textStyle="bold" />

    <!-- Task label -->
    <TextView
        android:id="@+id/widget_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="1dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:text="@string/widget_empty"
        android:textColor="#F4F1EA"
        android:textSize="14sp"
        android:textStyle="bold" />

    <!-- Spacer pushes the hero row to the bottom. Must be a RemoteViews-supported
         container (FrameLayout) — a bare <View> is NOT in the RemoteViews class list
         and makes the launcher fail to inflate the widget ("Can't load widget"). -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!-- Hero row: honest-finish clock (hero) + flat play button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="bottom">

        <TextView
            android:id="@+id/widget_hero"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:text=""
            android:textColor="#F4F1EA"
            android:fontFamily="monospace"
            android:textSize="27sp"
            android:textStyle="bold" />

        <FrameLayout
            android:id="@+id/widget_start"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="@drawable/widget_play_button">

            <ImageView
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:layout_gravity="center"
                android:layout_marginStart="1dp"
                android:src="@drawable/ic_widget_play"
                android:contentDescription="@string/widget_start" />
        </FrameLayout>
    </LinearLayout>
</LinearLayout>
